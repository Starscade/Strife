services:

  app:
    build: .
    environment:
      - STRIFE_INDEX
    ports:
      - "${STRIFE_PORT:-8080}:8080"
    volumes:
      - "${STRIFE_DB:-./db.sqlite3}:/var/lib/Strife/db.sqlite3"
      - "${STRIFE_ROOT:-./}:/srv/www"
